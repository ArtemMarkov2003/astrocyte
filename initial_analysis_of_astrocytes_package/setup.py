from setuptools import setup, find_packages

setup(
       name='initial-analysis-of-astrocytes-package',
       version='0.0.1',
       description='My awesome package',
       packages=find_packages(),
       install_requires=['numpy', 'matplotlib'],  
    )